import * as React from "react";

export const text = "Registers";
export const route = "/registers";
export class Component extends React.Component {
    render() {
        return <h1>Register Configuration</h1>;
    }
}